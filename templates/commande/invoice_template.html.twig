<!DOCTYPE html>
<html>
<head>
    <style>
        table { width: 100%; border-collapse: collapse; }
        th, td { border: 1px solid black; padding: 8px; text-align: left; }
        th { background-color: #f2f2f2; }
        .total { font-weight: bold; }
    </style>
</head>
<body>
    <h1>Facture #{{ commande.id }}</h1>
    <p>Date: {{ "now"|date("Y-m-d") }}</p>
<p>Client: {{ commande.livraison ? commande.livraison.adresseLivraison : 'Non spécifié' }}</p>    <h2>Articles</h2>
    <table>
        <tr>
            <th>Produit</th>
            <th>Quantité</th>
            <th>Prix unitaire</th>
            <th>Total</th>
        </tr>
        {% for produit in produits %}
            <tr>
                <td>{{ produit.nom }}</td>
                <td>{{ commande.produits|filter(p => p.id == produit.id)|length }}</td>
                <td>{{ produit.prix }} €</td>
                <td>{{ (produit.prix * (commande.produits|filter(p => p.id == produit.id)|length))|number_format(2) }} €</td>
            </tr>
        {% endfor %}
    </table>

    <p class="total">Total: {{ total|number_format(2) }} €</p>
</body>
</html>