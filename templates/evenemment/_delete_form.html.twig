<form method="post" action="{{ path('app_evenemment_delete', {'id': evenemment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evenemment.id) }}">
    <button class="btn">Supprimer</button>
</form>
