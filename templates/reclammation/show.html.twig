{% extends 'base.html.twig' %}

{% block title %}Reclammation{% endblock %}

{% block body %}
    <h1>Reclammation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reclammation.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ reclammation.description }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ reclammation.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reclammation_index') }}">back to list</a>

    <a href="{{ path('app_reclammation_edit', {'id': reclammation.id}) }}">edit</a>

    {{ include('reclammation/_delete_form.html.twig') }}
{% endblock %}
